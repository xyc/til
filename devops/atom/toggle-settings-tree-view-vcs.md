# Toggle settings tree view

To hide `git ignore`d files, in Settings -> Packages -> tree-view:
toggle Hide VCS ignored files.

There isn't a hot key for this. Maybe some hacks or write an extension.
